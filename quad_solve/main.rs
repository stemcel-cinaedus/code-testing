struct polynomial(i32, i32, i32);

fn main() {
    let mut input = String::new();
    let mut a: i32 = _;
    let mut b: i32 = _;
    let mut c: i32 =_;
    println!("Input a, b, and c of the quadratic polynomial, seperated by spaces.");
    io::stdin().read_line(&mut input_s).expect("Couldn't read input.");
    let mut str_vals: Vec<&str> = text.split(" ").collect();
    let quadratic_vals = vec!()
    for string_value in str_vals {
        let num = match num.trim().parse() {
            Ok(num) => num,
            Err(_) => panic,
        };
        quadratic_vals.append(num);
    }
}
